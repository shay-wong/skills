---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go.
disable-model-invocation: true
---

Use the installed model-invoked `grilling` and `domain-modeling` Skills together for this session.

Call the Skill tool once for each exact name when the current harness exposes it. Otherwise locate both Skills in the authoritative available-Skills catalog, read each complete `SKILL.md`, and apply both workflows together. Report a missing Skill instead of improvising only when neither route can find it.
