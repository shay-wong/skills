---
"shay-skills": patch
---

Dual-write `to-spec` and `to-tickets` artifacts to `.scratch` and through the installed `manage-panel` Skill when an active Panel or Jira planning context exists, recording each local relative path remotely and the returned Panel identifier and status locally. Route `implement` closeout through `review`, where `code-review` remains primary and the originating Spec or Ticket remains the acceptance contract, then use `manage-panel` to comment on and move an active Panel Issue to `in_review` while mirroring successful execution-state transitions to its `.scratch` ticket.

Keep composed Skills executable in harnesses without a Skill tool by reading their authoritative catalog entries directly, route beta `implement-spec` closeout through `review`, and replace stale continuous-loop references with the repository's installed RFC, quality, verification, eval, and harness-audit Skills.
