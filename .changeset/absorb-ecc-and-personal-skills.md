---
"shay-skills": minor
---

Publish the personal distribution as `shay-skills`. Absorb ECC and Shay's local workflows into the Matt-based collection; rename `setup-matt-pocock-skills` to `configure-skills` and the inherited `ask-matt` router to `ask-me`, both without compatibility aliases; keep Matt's `tdd`, `research`, `implement`, `code-review`, and `resolving-merge-conflicts` as canonical reusable entry points; restore `review` as the personal review orchestrator with catalog-first runtime discovery of installed complementary reviewers; fold `review-standards` policy into the `code-review` Standards axis; add `commit` and `worktree-clean`; organize non-overlapping ECC Skills by domain; add an external dependency installer with automatic, checklist, and skip modes for Codex plugins, Agent Skills, and global npm runtimes; and preserve every beta Skill under `skills/in-progress`.
