---
"mattpocock-skills": minor
---

Absorb ECC and Shay's local workflows into the Matt-based collection. Rename the inherited `ask-matt` router to `ask-me` with no compatibility alias; keep Matt's `tdd`, `research`, `implement`, `code-review`, and `resolving-merge-conflicts` as canonical reusable entry points; restore `review` as the personal review orchestrator with catalog-first runtime discovery of installed complementary reviewers; fold `review-standards` policy into the `code-review` Standards axis; add `commit` and `worktree-clean`; organize non-overlapping ECC Skills by domain; and preserve every beta Skill under `skills/in-progress`.
